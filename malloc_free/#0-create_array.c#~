#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 *
 *
 *
 *
 *
 *
 **/

char *create_array(unsigned int size, char c)
{
	char *array;

	array = malloc(size * sizeof(char));
	if (array == NULL)
	{
		return (NULL);
	}

	