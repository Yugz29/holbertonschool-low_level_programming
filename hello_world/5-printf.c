#include <stdio.h>

/**
 * main- It prints with proper grammar, but the outcome is a piece of art,
 *
 * Return: Always return 0
 *
 **/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
