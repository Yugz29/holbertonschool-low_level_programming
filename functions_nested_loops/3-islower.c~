#include "main.h"

/**
 * _islower - function that checks fo lowercase character
 *
 * Return: always return 0
 *
 **/

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
