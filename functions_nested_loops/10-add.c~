#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 *
 * int: declare variables that contain integers
 *
 * Return: the sum of a and b
 **/

int add(int, int)
{
	return (a + b);
}
